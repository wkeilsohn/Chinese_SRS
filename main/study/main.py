### These functions may become obsolete very quickly

def check_existing_users(user_name, user_db):
    try:
        user_data = 
        user_df = user_data[user_data["User_Name"] == user_name]
    except:
        starting_words = create_first_words()
    return user_df
